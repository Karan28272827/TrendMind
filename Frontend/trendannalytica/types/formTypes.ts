export interface SignupFormData {
    name: string;
    email: string;
    password: string;
    retypepassword: string;
  }

  export interface LoginFormData {
    email: string,
    password: string
  }
